//입력 폼을 만드는 파일
doctype html
html
  head
    meta(charset='utf-8')
  body
    h1
      a(href='/topic') Server Side JavaScript
    ul
      each topic in topics
        li
          a(href ='/topic/'+topic)= topic
    artcle
      form(action='/topic' method='post')
        p 
          input(type='text' name='date' placeholder='date')
        p
          textarea(name='description' placeholder='할 일')
        p 
          input(type='submit')